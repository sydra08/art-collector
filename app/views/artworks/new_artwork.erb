<div id="navbar">
  <a href="/collections/<%=current_collection.id%>" class="button-1">Back</a>
</div>

<h1>Add an artwork to your collection</h1>
<br>

<h3>Choose an existing artwork</h3>
<form action="/artworks" method="post">
  <select name="artwork_id" type="text" required>
    <option disabled selected value> -- select an artwork -- </option>
    <% @available_artworks.each do |a| %>
      <option value="<%=a.id%>"> <%=a.name%> by <%=a.artist.name unless a.artist.nil?%></option>
    <% end %>
  </select>

  <input type="submit" value="Add Artwork">
</form>

<br>

<h3>Or add a new artwork:</h3>
<form action="/artworks" method="post">
  <input type="hidden" name="artwork_id" value="">
  <label>Name: </label><input type="text" name="artwork[name]" placeholder="The Fountain" required> <br>
  <label>Year(s): </label><input type="text" name="artwork[year]" placeholder="1970-1972"> <br>
  <label>Artist: </label> <input type="text" name="artwork[artist]" placeholder="Caravaggio"> or
  <select name="artwork[artist]" type="text">
    <option disabled selected value> -- select an artist -- </option>
    <%Artist.all.each do |a|%>
      <option value="<%=a.name%>"> <%=a.name%> </option>
    <%end%>
  </select>

  <input type="submit" value="Add Artwork">
</form>
